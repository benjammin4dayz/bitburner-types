// this package doesn't actually export anything
// it just makes the NS namespace available for use in your JSDoc
// as a global type.

// Use the globally available type `NS` in your JSDoc

/** @param {NS} ns */
function main(ns) {
  // try it here!
}
